[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "microsoft-agents-protocol"
version = "0.1.0"
description = "Microsoft Agents Protocol - Add Agent Protocol routes to M365 Agents SDK"
readme = "README.md"
requires-python = ">=3.9"
license = {text = "MIT"}
authors = [
    {name = "Microsoft Corporation"}
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]

dependencies = [
    "aiohttp>=3.8.0",
    "microsoft-agents-protocol-abstractions>=0.2.0",
    "microsoft-agents-protocol-xml>=0.2.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "pytest-asyncio>=0.21.0",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["microsoft*"]

[tool.setuptools.package-dir]
"microsoft.agents.protocol" = "microsoft/agents/protocol"

[tool.setuptools.package-data]
"microsoft.agents.protocol" = ["*.py"]

[tool.pytest.ini_options]
markers = [
    "integration: Integration tests that test full end-to-end scenarios",
    "unit: Unit tests that test individual components in isolation",
]
testpaths = ["tests"]
asyncio_mode = "auto"
