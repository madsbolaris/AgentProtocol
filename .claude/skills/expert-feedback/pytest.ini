[pytest]
markers =
    recording: marks tests that generate LLM recordings (deselect with '-m "not recording"')
    integration: marks tests as integration tests
    requires_recordings: marks tests that require pre-recorded LLM data
    slow: marks tests as slow running

# Async test configuration
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
