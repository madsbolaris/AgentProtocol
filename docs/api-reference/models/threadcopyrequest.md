# ThreadCopyRequest

Thread Copy Request

<!-- GENERATED_START -->

## ThreadCopyRequest

Thread Copy Request

### Usage

Use Cases:
- "Try this approach instead" → Copy thread, continue with different strategy
- Testing agents: Same input conversation, multiple agent configurations
- Template threads: Copy starter thread for new conversations
- Conversation variations: Explore what-if scenarios

### Properties

| Property | Type | Required | Description |
|----------|------|----------|-------------|
| `includeHistory` | `boolean = true` | No | Whether to include full message history in copied thread. |
| `metadata` | `Record<unknown>` | No | Metadata for the new copied thread. |

---
<!-- GENERATED_END -->